import { ReactNode } from 'react';
declare function renderSmartValue(value: any): ReactNode;
declare function isIsoDate(value: string): boolean;
declare function formatLabel(key: string): string;
export { renderSmartValue, isIsoDate, formatLabel };
